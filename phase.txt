Phase 9 (Demo Polish & Submission) plan is ready—tying everything to the Frontier dApp Kit experience:

Demo-Ready UX Pass

Add loading/skeleton states and “connected via EVE Frontier” badges so every panel looks intentional inside the in-game shell.
Highlight wallet/assembly context from the dApp Kit across the hero + credits sections (e.g., “Logged in as <wallet> on tenant <assembly>”).
Layer in subtle animations (panel fades, timeline reveal) that match the existing cosmic theme without harming iframe performance.
Frontier Resource Integration

Feed the dApp Kit’s smart-object data into more panels (e.g., auto-filter reports by assembly if that metadata exists).
Surface Frontier resource links: a CTA to open the official assembly browser plus the Sui explorer proofs we already show.
Submission Package

Expand the demo storyline into a narrated script with timestamps and callouts to the UI components (credits panel, locked gates, Walrus proofs).
Write a README/demo guide covering setup, env toggles (GIN_DISABLE_ONCHAIN, etc.), and the “Ikora Corridor” walkthrough.
Capture screenshots/mockups for the dashboard hero + credits panel to include in the submission deck.
Optional Extras (time permitting)

Record automated Playwright flows for the full demo path (connect wallet → submit report → see credits → open gated panel).
Build a lightweight toast/notification system for Sui/Walrus fallback states, hooked into the dApp Kit connection status.